# variables- variables are nothing but, if we assign name to set of data is called variable
# VARNAME=DATA
a=10
# Acess the variable -$VARNAME
echo A- $a
echo C- $c
# a=10 is hardcoded manually however we need the data dynamically
# dynamically you can get in 2 ways
# 1.user input
# 2.Script find with other commands
# command substitution
# VARNAME=$(command)
# output of the command stored in variable